<resources>
    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/colorWindowBackground</item>
        <item name="android:actionMenuTextColor">@color/textColorWhite</item>
        <item name="android:textColor">@color/textColorBlack</item>
        <item name="android:editTextColor">@color/textColorBlack</item>
        <item name="android:listDivider">@color/listDivider</item>
        <item name="android:textColorPrimary">@color/textColorBlack</item>
        <item name="android:textColorPrimaryInverse">@color/textColorBlackHint</item>
        <item name="android:textColorHint">@color/textColorBlackHint</item>
        <item name="android:textColorSecondary">@color/textColorBlackLight</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseAppTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppToolBarTheme" parent="Widget.AppCompat.Toolbar">
        <!-- Customize your theme here. -->

    </style>

    <style name="textStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/dimen_padding</item>
        <item name="android:textColorHint">@color/textColorBlackHint</item>
        <item name="android:textColor">@color/textColorBlack</item>
    </style>

    <style name="toolBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

    <style name="TextInputLayoutStyle" parent="Widget.Design.TextInputLayout">
        <item name="hintTextAppearance">@style/TextInputHintTextAppearance</item>
        <item name="errorTextAppearance">@style/TextAppearance.Design.Error</item>
    </style>

    <style name="TextInputHintTextAppearance" parent="TextAppearance.Design.Hint">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/abc_text_size_caption_material</item>
    </style>

    <style name="appAlertDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:colorBackground">@color/colorWhite</item>
        <item name="android:windowTitleBackgroundStyle">@style/windowTitleBackgroundStyle</item>
    </style>

    <style name="windowTitleBackgroundStyle" parent="Base.DialogWindowTitleBackground.AppCompat">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="mProgress_circle" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:indeterminateDrawable">@drawable/progressbar_circle</item>
    </style>

</resources>
